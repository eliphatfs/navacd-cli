#pragma once
// NavACD standalone: minimal stub.  SparseDynamicOctree3 references
// TSparseGrid3 for its per-cell storage layer, which NavACD does not touch
// directly; the sparse dynamic octree path is used only via its simpler
// public API in the convex-decomposition pipeline.
